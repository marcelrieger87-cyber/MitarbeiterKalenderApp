<Window x:Class="MitarbeiterKalenderApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mitarbeiter Kalender" Height="720" Width="1200">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Content="Render Testmonat"
                    Padding="14,6"
                    Click="OnRenderTestClick"/>
            <TextBlock Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       Text="-> ruft CalendarRenderService auf und zeigt Ergebnis an"/>
        </StackPanel>

        <TextBox Grid.Row="2"
                 Name="OutputBox"
                 FontFamily="Consolas"
                 FontSize="13"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"/>
    </Grid>
</Window>
