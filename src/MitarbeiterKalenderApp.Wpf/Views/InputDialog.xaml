<Window x:Class="MitarbeiterKalenderApp.Wpf.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Eingabe" Height="170" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="SemiBold" Text="{Binding Prompt}"/>
        <TextBox Grid.Row="2" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" MinHeight="34"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Abbrechen" Width="110" Margin="0,0,8,0" Click="OnCancel"/>
            <Button Content="OK" Width="110" Click="OnOk"/>
        </StackPanel>
    </Grid>
</Window>
