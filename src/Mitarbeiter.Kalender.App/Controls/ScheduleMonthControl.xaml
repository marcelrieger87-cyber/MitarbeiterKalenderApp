<UserControl x:Class="Mitarbeiter.Kalender.App.Controls.CalendarMonthControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Border CornerRadius="14"
            Background="{DynamicResource Surface}"
            BorderBrush="{DynamicResource Border}"
            BorderThickness="1"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="(Legacy) Monatsansicht – aktuell nicht genutzt"
                       Foreground="{DynamicResource TextMuted}"
                       FontWeight="SemiBold"/>

            <ScrollViewer Grid.Row="2"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Dieses Control ist nur noch für die Kompilierung vorhanden."
                               Foreground="{DynamicResource Text}"
                               Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding MonthTitle}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource Text}"/>

                    <TextBlock Text="Die neue Excel-Ansicht kommt aus: ScheduleMonthControl"
                               Foreground="{DynamicResource TextMuted}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
