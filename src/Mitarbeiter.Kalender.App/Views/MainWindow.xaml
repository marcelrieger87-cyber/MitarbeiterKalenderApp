<Window x:Class="Mitarbeiter.Kalender.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Mitarbeiter.Kalender.App.Controls"
        mc:Ignorable="d"
        Title="Mitarbeiter Kalender" Width="1240" Height="800" WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header (Excel-like) -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical" Grid.Column="0">
                    <TextBlock Text="Mitarbeiter-Kalender" FontSize="20" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text="Planung · Termine · Serien · Abwesenheit" Foreground="#CCFFFFFF" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <StackPanel Width="260" Margin="0,0,12,0">
                        <TextBlock Text="Mitarbeiter" Foreground="#CCFFFFFF" Margin="2,0,0,4" />
                        <ComboBox ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" />
                    </StackPanel>

                    <StackPanel Width="320" Margin="0,0,12,0">
                        <TextBlock Text="Kunde (Filter / Neueingabe)" Foreground="#CCFFFFFF" Margin="2,0,0,4" />
                        <TextBox Text="{Binding CustomerText, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel Width="200" Margin="0,0,12,0">
                        <TextBlock Text="Monat" Foreground="#CCFFFFFF" Margin="2,0,0,4" />
                        <DatePicker SelectedDate="{Binding SelectedMonth}" DisplayDateStart="2000-01-01" DisplayDateEnd="2100-12-31" />
                    </StackPanel>
                </StackPanel>

                <WrapPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" ItemHeight="44" ItemWidth="140">
                    <Button Style="{StaticResource PrimaryButton}" Content="◀" Width="44" Command="{Binding PrevMonthCommand}" Margin="0,0,10,0" />
                    <Button Style="{StaticResource PrimaryButton}" Content="▶" Width="44" Command="{Binding NextMonthCommand}" Margin="0,0,18,0" />

                    <Button Style="{StaticResource PrimaryButton}" Content="Heute" Command="{Binding TodayCommand}" Margin="0,0,10,0" />
                    <Button Style="{StaticResource GoodButton}" Content="Termin" Command="{Binding AddAppointmentCommand}" Margin="0,0,10,0" />
                    <Button Style="{StaticResource WarnButton}" Content="Abwesenheit" Command="{Binding AddAbsenceCommand}" Margin="0,0,10,0" />
                    <Button Style="{StaticResource BadButton}" Content="Demo" Command="{Binding SeedDemoCommand}" />
                </WrapPanel>
            </Grid>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="1" Margin="16">
            <Border Style="{StaticResource Card}">
                <controls:CalendarMonthControl ItemsSource="{Binding Cells}" SelectDayCommand="{Binding SelectDayCommand}" />
            </Border>
        </Grid>
    </Grid>
</Window>
