<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Excel-ish palette (can be tuned) -->
    <Color x:Key="CBackground">#FFF5F7FA</Color>
    <Color x:Key="CSurface">#FFFFFFFF</Color>
    <Color x:Key="CHeader">#FF1F4E79</Color>
    <Color x:Key="CHeader2">#FF2E75B6</Color>

    <Color x:Key="CAccent">#FF00A3E0</Color>
    <Color x:Key="CGood">#FF2E7D32</Color>
    <Color x:Key="CWarn">#FFEF6C00</Color>
    <Color x:Key="CBad">#FFC62828</Color>

    <SolidColorBrush x:Key="Bg" Color="{StaticResource CBackground}" />
    <SolidColorBrush x:Key="Surface" Color="{StaticResource CSurface}" />
    <SolidColorBrush x:Key="HeaderBrush" Color="{StaticResource CHeader}" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource CAccent}" />
    <SolidColorBrush x:Key="GoodBrush" Color="{StaticResource CGood}" />
    <SolidColorBrush x:Key="WarnBrush" Color="{StaticResource CWarn}" />
    <SolidColorBrush x:Key="BadBrush" Color="{StaticResource CBad}" />

    <CornerRadius x:Key="RCard">16</CornerRadius>
    <CornerRadius x:Key="RButton">10</CornerRadius>

    <DropShadowEffect x:Key="ShadowSoft" BlurRadius="18" ShadowDepth="2" Opacity="0.15" />

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Bg}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{StaticResource Surface}" CornerRadius="10" BorderBrush="#22000000" BorderThickness="1">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="BorderBrush" Value="#22000000" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="{StaticResource Surface}" />
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="BorderBrush" Value="#00000000" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="{StaticResource RButton}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.9" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.8" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GoodButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource GoodBrush}" />
    </Style>

    <Style x:Key="WarnButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource WarnBrush}" />
    </Style>

    <Style x:Key="BadButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource BadBrush}" />
    </Style>

    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Surface}" />
        <Setter Property="CornerRadius" Value="{StaticResource RCard}" />
        <Setter Property="Padding" Value="14" />
        <Setter Property="Effect" Value="{StaticResource ShadowSoft}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="#11000000" />
    </Style>

</ResourceDictionary>
